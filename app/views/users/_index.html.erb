<table class="table table-hover text-center">
  <thead>
    <tr>
      <th colspan="2">ユーザ</th>
      <th>投稿数</th>
      <th>フォロー</th>
      <th>フォロワー</th>
      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% users.each do |user| %>
      <tr>
        <td class="align-middle">
          <%= link_to user_path(user) do %>
            <%= attachment_image_tag(user, :profile_image, size:'40x40', fallback: "cheer.jpeg", style: "border-radius: 50%") %>
          <% end %>
        </td>
        <td class="align-middle"><strong><%= user.name %></strong></td>
        <td class="align-middle"><%= user.posts.count %></td>
        <td class="align-middle"><%= link_to user.followings.count, user_followings_path(user) %></td>
        <td class="align-middle"><%= link_to user.followers.count, user_followers_path(user) %></td>
        <td class="align-middle">
          <% if user != current_user %>
            <% if current_user.following?(user) %>
              <%= link_to "フォロー外す", user_relationships_path(user), method: :delete, class: "btn btn-primary btn-sm" %>
            <% else %>
              <%= link_to "フォローする", user_relationships_path(user), method: :post, class: "btn btn-success btn-sm" %>
            <% end %>
          <% end %>
        </td>
        <td class="align-middle"><%= link_to "", user_path(user), class: "fas fa-user-plus" %></td>
      </tr>
    <% end %>
  </tbody>
</table>