<% post.post_comments.each do |post_comment| %>
  <% post_comment_user = post_comment.user %>
  <tr>
  	<td>
  		<%= link_to user_path(post_comment_user), class: "user_#{post_comment_user.id}" do %>
    		<%= attachment_image_tag post_comment_user, :profile_image, fallback: "cheer.png", size: "50x50" %>
    		<%= post_comment_user.name %>
  		<% end %>
  	</td>
  	<td class="align-middle"><%= post_comment.comment %></td>
  	<% if current_user == post_comment_user %>
    	<td class="align-middle"><%= link_to "", post_post_comment_path(post_comment.post_id, post_comment.id), method: :delete, remote: true, class: "fas fa-trash" %></td>
  	<% else %>
  	  <td></td>
  	<% end %>
  </tr>
<% end %>